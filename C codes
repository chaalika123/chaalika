// Program to calculate area of a circle

#include <stdio.h>

void main()
{
   float radius, area;

      printf("Enter radius :");
      scanf("%f",&radius);

      area = 3.14 * radius * radius;
      printf("Area for a circle with radius %8.2f is %8.2f",radius, area);
}
